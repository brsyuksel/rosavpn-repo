VERSION =
ARCH = 

ifeq ($(VERSION),)
	$(error VERSION is required.)
endif

ifeq ($(ARCH),)
	$(error ARCH is required.)
endif

test:
	@echo "testing udp2raw package..."

package:
	@echo "packaging udp2raw..."