VERSION =
ARCH = 

ifeq ($(VERSION),)
	$(error VERSION is required.)
endif

ifeq ($(ARCH),)
	$(error ARCH is required.)
endif

test:
	@echo "testing tor package..."

package:
	@echo "packaging tor..."